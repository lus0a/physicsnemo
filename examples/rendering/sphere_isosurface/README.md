# Sphere Isosurface Rendering

This example builds a scalar field plus a `uint8` RGB volume for a moving sphere
and renders it with the fused `isosurface_render` functional.

Run with:

```bash
python render_sphere.py
```

The script writes PNG frames and an animated GIF to
`outputs_sphere_isosurface/`.
