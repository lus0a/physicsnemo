# Mesh Raycast Rendering

This example renders a colored rotating cube with the Warp-backed
`mesh_raycast` functional.

Run with:

```bash
python render_mesh.py
```

The script writes PNG frames and an animated GIF to `outputs_mesh_raycast/`.
